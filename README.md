# AstraRise backend
Push this folder to GitHub. Deploy on Render: New Web Service → connect repo → Build: `npm install` → Start: `npm start`. Copy the URL Render gives you and put it in the frontend's index.html before deploying the frontend to Vercel.
